***REMOVED*** ── Secrets ──────────────────────────────────────────────
config_local.py
***REMOVED*** Matériel de CA interne (mTLS) : clés privées — JAMAIS versionné (cf. app/ca.py, TLS_DIR).
tls/
*.key

***REMOVED*** ── Base de données runtime ───────────────────────────────
*.db
***REMOVED*** Sauvegardes de base : *.db ne les attrape PAS (suffixe après l'extension).
***REMOVED*** Deux d'entre elles ont été versionnées par ce trou — elles contenaient
***REMOVED*** flask_secret_key, le mot de passe SMTP et les hash de comptes réels.
*.db.bak*
*.bak-*
*-shm
*-wal
**/.clips.db
db_testplan.json
db_testplan.json.tmp

***REMOVED*** ── Logs ─────────────────────────────────────────────────
*.log
*.log.*

***REMOVED*** ── ISO d'enrôlement cachées (service iLO Virtual Media) ──
node_iso_cache/
***REMOVED*** ── ISO netinst Debian source téléchargée (champ « depuis une URL ») ──
iso_src/
***REMOVED*** ── Arbre netboot Debian extrait (boot réseau PXE / UEFI HTTP Boot) ──
pxe_root/
.netboot.tar.gz.part

***REMOVED*** ── Python ───────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo

***REMOVED*** ── Environnement virtuel ─────────────────────────────────
venv/

***REMOVED*** ── Sauvegardes automatiques ──────────────────────────────
backups/

***REMOVED*** ── Réplica HA stagé (snapshot DB reçu par un standby — runtime) ──
ha_staging/

***REMOVED*** ── Uploads utilisateur (assets runtime, pas du code) ────
static/uploads/

***REMOVED*** ── Ancienne codebase (référence archivée, non versionnée) ─
old/

***REMOVED*** ── Mémoire interne (dépôt PRIVÉ séparé, cloné ici) ───────
***REMOVED*** PAS un sous-module : un sous-module privé casse le clone --recursive du public.
interne/

***REMOVED*** ── Plans / mémoire / skills Claude Code ─────────────────
.claude/
.agents/

***REMOVED*** ── État runtime (rings d'historique flushés, pas du code) ─
ptp_stats.json
node_health_stats.json
clocks_hist.json
flux_pannes.json
flux_pannes.json.tmp

***REMOVED*** ── OS ───────────────────────────────────────────────────
.DS_Store
Thumbs.db

***REMOVED*** ── Artefacts de distribution ────────────────────────────
bobistudio_dist.tar.gz
dist/
vendor/
build_info.json
UPDATE_PENDING

***REMOVED*** ── Locks runtime ────────────────────────────────────────
db_testplan.json.lock
*.db-journal
*.db-wal
*.db-shm
venv

***REMOVED*** ── État runtime (épisodes de surveillance, profils CPU) ──
episodes_*.json
cpu_pressure_stats.json

***REMOVED*** ── Outils de profilage téléchargés ──
py_spy-*.whl

***REMOVED*** Captures d'écran de travail (site vitrine) : elles portent des IP, des noms de nœuds
***REMOVED*** et le plan multicast — jamais dans un dépôt public.
_captures/
